extends layout

block content
    .container
        .row
            .col-md-offset-3.col-md-6
                h2.h2
                    p.bg-danger=message
                form.form-horizontal(method="POST")
                    span.heading АВТОРИЗАЦИЯ
                    .form-group
                        input#inputEmail.form-control(type='text', placeholder='Введите логин', name="username")
                        i.fa.fa-user
                    .form-group.help
                        input#inputPassword.form-control(type='password', placeholder='Ведите пароль', name="password")
                        i.fa.fa-lock
                        a.fa.fa-question-circle(href='#')
                        button.btn.btn-default(type='submit') ВХОД
